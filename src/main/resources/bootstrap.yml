spring:
   cloud:
      config:
         name: lyz
         profile: prod
         label: master 
         discovery: 
            enable: true
            serviceId: springcloud-config-server 
eureka:
   client:
      serviceUrl:
         defaultZone: http://localhost:8888/eureka/
